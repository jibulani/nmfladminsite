// $('[data-toggle=confirmation]').load('.confirmation').confirmation({
// 	rootSelector: '[data-toggle=confirmation]',
// });
// $(document).find('.delete').on('confirmed.bs.confirmation', function() {
//     $(this).closest(".confirmation").confirmation('show');
// });
//('[data-toggle="confirmation"]').confirmation('show');
//$(this).confirmation('show');
//e.preventDefault();

           // alert("Deleted! Do not forget to do some ajax to sync with backend :)");

   //          $('[data-toggle=confirmation]').load('.confirmation').confirmation({
			// 	rootSelector: '[data-toggle=confirmation]',
			// });
			// $(document).find('[data-toggle="confirmation"]').confirmation();
            // $(document).on("click", ".delete button", function () {
            // 	$(document).confirmation('show');
            // 	if ($(document).on("click", '[data-apply="confirmation"]')){
            // 	}
            // });
   			// if ($(this).confirmation('show').on("click", '[data-apply="confirmation"]')){
   			// 	alert("Deleted! Do not forget to do some ajax to sync with backend :)");
   			// 	var nRow = $(this).parents('tr')[0];
      //       	oTable.fnDeleteRow(nRow);
      //       	alert("Deleted! Do not forget to do some ajax to sync with backend :)");
   			// }
   			// var solution = document.getElementsByClassName('btn-success');
   			// alert(solution);
   			// solution.onclick = function(){
   			// 	var nRow = $(this).parents('tr')[0];
      //       	oTable.fnDeleteRow(nRow);
      //       	return;
   			// }

   			//$(this).confirmation('show');

   			// alert($(this).confirmation('show').on("click", '[data-apply="confirmation"]'));
   			// if ($(this).confirmation('show').on("click", '[data-apply="confirmation"]')){
   			// 	//alert($(this).confirmation('show').on("click", '[data-apply="confirmation"]'));
   			// 	// var nRow = $(that).parents('tr')[0];
      //  //      	oTable.fnDeleteRow(nRow);
      //  //      	return;
   			// }
   			// else {
   			// 	return;
   			// }
   // //          	$(this).closest(".confirmation").confirmation('show');
   // //          });
   // 			if ($(this).confirmation('show'). === 0){
   // 				counter++;
   // 				return;
   // 			}
   // 			else{
   // 				counter = 0;
   // 			}
            // var nRow = $(this).parents('tr')[0];
            // oTable.fnDeleteRow(nRow);
            // alert("Deleted! Do not forget to do some ajax to sync with backend :)");




            //VERSION 2.0
   //          var counter = 0;
   //      table.on('click', '.delete', function (e) {
   //          e.preventDefault();
   // //          $('[data-toggle=confirmation]').load('.confirmation').confirmation({
			// // 	rootSelector: '[data-toggle=confirmation]',
			// // });
			// // $(document).find('[data-toggle="confirmation"]').confirmation();
   // //          $(document).on("click", ".delete button", function () {
   // //          	$(this).closest(".confirmation").confirmation('show');
   // //          });
   // 			$(this).confirmation('show');
   			
   // 			if ($(this).confirmation('show'). === 0){
   // 				counter++;
   // 				return;
   // 			}
   // 			else{
   // 				counter = 0;
   // 			}
   //          var nRow = $(this).parents('tr')[0];
   //          oTable.fnDeleteRow(nRow);
   //          alert("Deleted! Do not forget to do some ajax to sync with backend :)");
   //      });


  // $(document).confirmation('show');